services:

  db:
    image: postgres:16
    container_name: quick_commerce_db
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # ports:
    #   - "5432:5432"

  redis:
    image: redis:7
    container_name: quick_commerce_redis
    restart: unless-stopped
    ports:
      - "6379:6379"

  web:
    build: .
    container_name: quick_commerce_web
    restart: unless-stopped
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis

  celery_worker:
    build: .
    container_name: quick_commerce_celery_worker
    restart: unless-stopped
    env_file:
      - .env
    command: celery -A config worker --loglevel=info
    volumes:
      - .:/app
    depends_on:
      - db
      - redis

  celery_beat:
    build: .
    container_name: quick_commerce_celery_beat
    restart: unless-stopped
    env_file:
      - .env
    command: celery -A config beat --loglevel=info
    volumes:
      - .:/app
    depends_on:
      - db
      - redis

volumes:
  postgres_data: